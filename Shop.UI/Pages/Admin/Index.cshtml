@page
@model IndexModel
@{
}

<div id="app" class="container">
    <div v-if="!editing">
        <button class="button" @@click="newProduct">Dodaj nowy produkt</button>
        <table class="table">
            <tr>
                <th>Id</th>
                <th>Product</th>
                <th>Value</th>
                <th></th>
                <th></th>
            </tr>
            <tr v-for="(product, index) in products">
                <td>{{product.id}}</td>
                <td>{{product.name}}</td>
                <td>{{product.value}}</td>
                <td><a @@click="editProduct(product.id, index)">Edytuj</a></td>
                <td><a @@click="deleteProduct(product.id, index)">Usuń</a></td>
            </tr>
        </table>
    </div>

    <div v-else>
        <div class="field">
            <label class="label">Nazwa</label>
            <div class="control">
                <input class="input" v-model="productModel.name" />
            </div>
        </div>

        <div class="field">
            <label class="label">Product Description</label>
            <div class="control">
                <input class="input" v-model="productModel.description" />
            </div>
        </div>

        <div class="field">
            <label class="label">Wartość</label>
            <div class="control">
                <input class="input" v-model="productModel.value" />
            </div>
        </div>
        <button class="button is-success" @@click="createProduct" v-if="!productModel.id">Stwórz produkt</button>
        <button class="button is-warning" @@click="updateProduct" v-else>Update produkt</button>
        <button class="button" @@click="cancel">Anuluj</button>
    </div>
</div>


@section scripts{
    <script src="~/js/admin/products.js"></script>
}